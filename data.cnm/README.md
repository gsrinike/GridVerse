# data.cnm

Pure Java transport and domain records for import, IIDM-neutral summaries, load flow, security, sensitivity, RAO, remedial actions, workflow state, and result views. It depends only on serialization annotations and exposes no framework or adapter types. Run `mvn -pl data.cnm -am test`.
