# com.powsbl

The only boundary permitted to bind official PowSyBl APIs. Public interfaces accept and return `data.cnm` records only. Runtime implementations and version alignment can be supplied here without changing services or OpenAPI. Run `mvn -pl com.powsbl -am test`.
