# com.solver

Solver-neutral optimization models and the isolated SCIP-compatible boundary. Executable path, time limit, gap, threads, and fallback are runtime configuration. No solver-native type can cross this module. Run `mvn -pl com.solver -am test`.
