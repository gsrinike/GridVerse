# srv.rsa.services

Owns workflow state, review/application of proposed remedial actions, and before/after result visualization data. Computation and optimization remain separate deployables; this service persists workflow metadata and publishes transitions. Run `mvn -pl srv.rsa.services -am test`.
